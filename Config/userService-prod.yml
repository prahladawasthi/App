spring:
  application:
    name: userService
  cloud: 
    config: 
      uri: http://localhost:8800
    

userService: 
  getUser: 
    url: 
      mapping: /
      
eureka: 
  instance: 
    appname: ${spring.application.name}
  client: 
    service-url: 
      defaultZone: http://localhost:9090/eureka/
